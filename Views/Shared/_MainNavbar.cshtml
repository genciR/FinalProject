<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
    <a class="navbar-brand" href="#">SmartShop</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="btn btn-dark" asp-controller="Home" asp-action="Index">Home</a>
            </li>
            <li class="nav-item">
                <a class="btn btn-dark" asp-controller="Phone" asp-action="List">Smartphones</a>
            </li>
        </ul>
            @if(User.Identity.IsAuthenticated){
                 <div class="d-flex align-items-center">
                    <a class="btn btn-dark" asp-controller="ShoppingCart" asp-action="Index">Shopping Cart</a>
                </div>
            }
           
            @if(User.Identity.IsAuthenticated){
                 <div class="d-flex align-items-center">
                    <a class="nav-item btn btn-dark disabled">User: @User.Identity.Name</a>
                </div>
            }

            @if (!User.Identity.IsAuthenticated)
            {
                <div class="d-flex align-items-center">
                    <a class="nav-item btn btn-dark bg-gradient" asp-area="Identity" asp-page="/Account/Login">Sign In</a>
                </div>
            }
            @if (User.Identity.IsAuthenticated)
            {
                <div class="d-flex align-items-center">
                    <a class="nav-item btn btn-dark bg-gradient" asp-area="Identity" asp-page="/Account/Logout">Logout</a>
                </div>
            }
     </div>
    </div>
</nav>